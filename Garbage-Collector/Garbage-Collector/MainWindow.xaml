<Window x:Class="Garbage_Collector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Garbage_Collector.ViewModel"
        xmlns:Menu="clr-namespace:Garbage_Collector.Utilities"
        mc:Ignorable="d"
        Title="MainWindow"
        Width="1000"
        Height="800"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        MouseDown="Window_MouseDown"
        Style="{StaticResource MaterialDesignWindow}">

    <Window.DataContext>
        <vm:NavigationVM />
    </Window.DataContext>

    <Grid Margin="10">
        <!-- Border um das gesamte Fenster -->
        <Border Background="#212529" CornerRadius="19" />

        <Grid>
           
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
              
                <ColumnDefinition Width="*" />
              
            </Grid.ColumnDefinitions>

            <!-- Navigation Panel -->
            
                <Border Background="#272B2F" CornerRadius="20,0,0,20" />
                <StackPanel VerticalAlignment="Center">
                    <!-- Home Button -->
                    <Menu:Btn Style="{StaticResource BtnStyle}"
                              Command="{Binding HomeCommand}"
                              IsChecked="True">
                        <Grid>
                            <Image Source="Images/icon_home.png" Style="{StaticResource Image_Style}" />
                            <TextBlock Text="Home" Style="{StaticResource TextStyle_NavBar}" />
                        </Grid>
                    </Menu:Btn>

                    <!-- Cleanup Button -->
                    <Menu:Btn Style="{StaticResource BtnStyle}"
                              Command="{Binding CleanupCommand}">
                        <Grid>
                            <Image Source="Images/icon_cleanup.png" Style="{StaticResource Image_Style}" />
                            <TextBlock Text="Cleanup" Style="{StaticResource TextStyle_NavBar}" />
                        </Grid>
                    </Menu:Btn>

                    <!-- Settings Button -->
                    <Menu:Btn Style="{StaticResource BtnStyle}"
                              Command="{Binding SettingsCommand}">
                        <Grid>
                            <Image Source="Images/icon_settings.png" Style="{StaticResource Image_Style}" />
                            <TextBlock Text="Einstellungen" Style="{StaticResource TextStyle_NavBar}" />
                        </Grid>
                    </Menu:Btn>

                    <!-- Information Button -->
                    <Menu:Btn Style="{StaticResource BtnStyle}"
                              Command="{Binding InformationCommand}">
                        <Grid>
                            <Image Source="Images/icon_info.png" Style="{StaticResource Image_Style}" />
                            <TextBlock Text="Informationen" Style="{StaticResource TextStyle_NavBar}" />
                        </Grid>
                    </Menu:Btn>

                    <!-- Administration Button -->
                    <Menu:Btn Style="{StaticResource BtnStyle}"
                              Command="{Binding AdministrationCommand}"
                              Visibility="{Binding Source={x:Static vm:LoginVM.CurrentUserName}, Converter={StaticResource AdminVisibilityConverter}}">
                        <Grid>
                            <Image Source="Images/icon_admin.png" Style="{StaticResource Image_Style}" />
                            <TextBlock Text="Administration" Style="{StaticResource TextStyle_NavBar}" />
                        </Grid>
                    </Menu:Btn>

               
        
            </StackPanel>

            
            
            
            <StackPanel VerticalAlignment="Bottom" Margin="10">
                <Button Command="{Binding LogoutCommand}" HorizontalAlignment="Left" Background="Transparent" BorderBrush="Transparent">
                    <Image Source="Images/icon_logout.png" Style="{StaticResource LogoutImage_Style}" />
                </Button>
            </StackPanel>



            <Grid Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <ContentControl x:Name="Pages" Content="{Binding CurrentView}" />
            </Grid>

        </Grid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="MinimizeApp" Style="{StaticResource MinimizeApp}" Click="MinimizeApp_Click" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10"/>
            <Button x:Name="CloseApp" Style="{StaticResource ExitApp}" Click="CloseApp_Click" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10"/>
        </StackPanel>
        
    </Grid>

</Window>
