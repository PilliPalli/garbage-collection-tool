<Window x:Class="Garbage_Collector.View.Register"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.microsoft.com/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Garbage_Collector.ViewModel"
        Title="Register" Height="600" Width="850"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        MouseDown="Window_MouseDown"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}">

    <Window.DataContext>
        <vm:RegisterVM/>
    </Window.DataContext>

    <Border Background="#212529" CornerRadius="19">

        <Grid>
            <Grid.Effect>
                <DropShadowEffect BlurRadius="30" ShadowDepth="1" Opacity="0.4"/>
            </Grid.Effect>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="1.22*"/>
            </Grid.ColumnDefinitions>

            <Border Background="#272B2F" CornerRadius="10 0 0 10" ClipToBounds="True">
                <Grid>
                    <StackPanel Margin="0 120 0 0" VerticalAlignment="Top">
                        <TextBlock Text="Registrierung" FontSize="46" FontWeight="Light"  TextAlignment="Center"/>
                        
                    </StackPanel>
                    <Ellipse Width="300" Height="300" VerticalAlignment="Bottom" Fill="#1D4E89" Margin="-450 0 0 30" Opacity="0.3"/>
                    <Ellipse Width="400" Height="400" VerticalAlignment="Bottom" Fill="#5CB3FF" Margin="-320 0 0 -120" Opacity="0.3"/>
                    <Ellipse Width="250" Height="250" VerticalAlignment="Bottom" Fill="#68B3F4" Margin="-380 0 0 -120"/>
                    <Ellipse Width="400" Height="400" VerticalAlignment="Bottom" Fill="#4EA9F2" Margin="280 0 0 -70" Opacity="0.8"/>
                </Grid>
            </Border>
            <Image Source="\Images\logo_CleanUP.PNG" Margin="110,270,90,82" />

            <Border Grid.Column="1" Background="#212529" CornerRadius="0 10 10 0" MouseDown="Window_MouseDown">
                <Grid>
                    <Button x:Name="CloseApp" Style="{StaticResource ExitApp}" Click="CloseApp_Click"/>
                    <StackPanel VerticalAlignment="Center" Margin="75 0">
                        <TextBox materialDesign:HintAssist.FloatingOffset="0,-20" Margin="20" materialDesign:HintAssist.Hint="Benutzername" Text="{Binding Username}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                        <PasswordBox x:Name="passwordBox1"  materialDesign:HintAssist.FloatingOffset="0,-20" Margin="20" materialDesign:HintAssist.Hint="Passwort" PasswordChanged="PasswordBox_PasswordChanged" Style="{StaticResource MaterialDesignFloatingHintRevealPasswordBox}" />
                        <PasswordBox x:Name="passwordBox2"  materialDesign:HintAssist.FloatingOffset="0,-20" Margin="20" materialDesign:HintAssist.Hint="Passwort wiederholen" PasswordChanged="ConfirmPasswordBox_PasswordChanged"  Style="{StaticResource MaterialDesignFloatingHintRevealPasswordBox}" />

                        <Button Content="Account erstellen" Command="{Binding RegisterCommand}" Background="#339AF0" Foreground="#ffffff"    Margin="5 20 0 0"/>
                        <Button Content="Zurück zum Login" Command="{Binding BackToLoginCommand}"  CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"  Margin="5 20 0 0" Style="{StaticResource MaterialDesignFlatButton}"/>

                      

                    </StackPanel>
                </Grid>

            </Border>




        </Grid>

    </Border>
</Window>